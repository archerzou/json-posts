import React from "react";

import { Navbar } from "reactstrap";

const Header = () => {
  return (
    <Navbar className="text-white bg-primary" light expand="xs" >
      <h1 className="mx-auto">Posts Search</h1>
    </Navbar>
  );
};

export default Header;
